/target

# Reference implementations, read-only and not ours to carry.
/PhpSpreadsheet/
/excelize-master/

# Output of the example converters.
/tests/test1_out*.xlsx

# Third-party sources kept alongside for comparison (BetterOffice).
tmp/

# Real workbooks for the corpus test: large, and not all of them ours to
# publish. See tests/corpus.rs.
tests/corpus/
